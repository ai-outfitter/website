---
title: Own the agent lifecycle
description: Open, vendor-neutral tooling for composing, running, governing, and improving coding agents.
template: splash
hero:
  title: Own the agent lifecycle.
  tagline: Compose agents as reviewed configuration. Run them from a laptop, CI, or a cluster. Keep the evidence and improve what happens next.
  actions:
    - text: Start your first software factory
      link: /docs/start/
      icon: rocket
      variant: primary
    - text: Start with the CLI
      link: /docs/outfitter/documentation/getting-started/
      icon: right-arrow
      variant: minimal
---

<p class="eyebrow">Your first software factory · three steps</p>

## Assign an issue. Get a reviewed pull request.

Install the GitHub App on one repository. It opens a pull request adding a
single workflow file; merge it. Assign any issue to the agent login, and your
own GitHub Actions implement it, review it adversarially, and hand the App a
verdict to merge on. Nothing else is added to your repository.

<div class="signal-grid">
  <div>
    <span class="number">01 / INSTALL</span>
    <h3>One click, one file</h3>
    <p><a href="https://github.com/apps/ai-outfitter">Install the App</a>, pick the repository, merge the workflow pull request it opens.</p>
  </div>
  <div>
    <span class="number">02 / ASSIGN</span>
    <h3>Like assigning a teammate</h3>
    <p>Assign an issue to the agent login. The run starts in your Actions within seconds.</p>
  </div>
  <div>
    <span class="number">03 / REVIEW</span>
    <h3>A second agent pushes back</h3>
    <p>An adversarial review lands with findings and a verdict. Approve merges; request-changes revises once, then a human decides.</p>
  </div>
</div>

[Start your first software factory →](/docs/start/)

<p class="eyebrow">One configuration · every surface</p>

## Move the control point outward

AI Outfitter is an open toolchain for the part after “try a coding agent.” Keep
agent context, skills, tools, and permissions in a reviewable `.agents/` tree,
then reuse the same composition wherever the work runs.

<div class="signal-grid">
  <div>
    <span class="number">01 / COMPOSE</span>
    <h3>Configuration you own</h3>
    <p>Layer personal, organization, and project catalogs by slug. Pin remote sources and review changes like code.</p>
  </div>
  <div>
    <span class="number">02 / OPERATE</span>
    <h3>Work beyond one laptop</h3>
    <p>Run the same profile interactively, in GitHub Actions, or as a resident Kubernetes agent.</p>
  </div>
  <div>
    <span class="number">03 / VERIFY</span>
    <h3>Trust through evidence</h3>
    <p>Measure outcomes, preserve session evidence, and make improvement a checked loop instead of a guess.</p>
  </div>
</div>

<p class="eyebrow">The adoption ramp</p>

## Climb without rebuilding

Start with one delegated task. Share the configuration that worked. Move it to
an automated surface. Add governance and durable evidence when the system earns
more authority. Each rung keeps the useful parts of the last.

<div class="status-strip">
  <span>Assisted</span>
  <span>Delegated</span>
  <span>Automated</span>
  <span>Governed</span>
  <span>Self-improving</span>
</div>

[See how to move the control point outward →](/docs/adoption-ramp/)

<p class="eyebrow">Open by construction</p>

## Bring your harness. Keep your configuration.

Outfitter does not invent a private configuration format. The `.agents/`
directory remains useful on its own and can target Pi, Claude Code, Codex, and
the execution surfaces your organization already operates.

```sh
npx @ai-outfitter/outfitter
```

[Read the getting started guide →](/docs/outfitter/documentation/getting-started/)
