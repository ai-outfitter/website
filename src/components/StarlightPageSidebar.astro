---
import DefaultPageSidebar from '@astrojs/starlight/components/PageSidebar.astro';

const isWorkflow = Astro.url.pathname.startsWith('/docs/workflows/');
---

{!isWorkflow && <DefaultPageSidebar />}
