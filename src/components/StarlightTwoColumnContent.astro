---
import DefaultTwoColumnContent from '@astrojs/starlight/components/TwoColumnContent.astro';

const isWorkflow = Astro.url.pathname.startsWith('/docs/workflows/');
---

{
  isWorkflow ? (
    <div class="workflow-main-pane">
      <slot />
    </div>
  ) : (
    <DefaultTwoColumnContent>
      <slot name="right-sidebar" slot="right-sidebar" />
      <slot />
    </DefaultTwoColumnContent>
  )
}

<style is:global>
  .workflow-main-pane {
    --sl-content-width: 100%;
    width: 100%;
  }

  @media (max-width: 71.999rem) {
    .main-frame:has(.workflow-main-pane) {
      padding-top: var(--sl-nav-height);
    }
  }
</style>
